# POC P1/P2 验证结果 — 2026-09-03

## 环境

- macOS Docker Desktop (ARM), Ubuntu 24.04 容器
- OpenWrt Image Builder: `openwrt-imagebuilder-25.12.5-armsr-armv8.Linux-x86_64`
- Docker named volume (`tn-poc`) 解决大小写敏感问题
- 覆盖层：13 个文件（本地 `/tmp/tn-overlay`，映射到容器 `/tmp/overlay`）

## P1: rootfs.tar.gz 产出 ✅

```
openwrt-25.12.5-armsr-armv8-generic-rootfs.tar.gz  (5.6 MB)
openwrt-25.12.5-armsr-armv8-generic-targz-rootfs.tar.gz  (5.6 MB)
```

IB 默认产出两个 rootfs.tar.gz（generic + targz），ophub remake 输入天然可用。

## P2: 覆盖层注入验证 ✅

| 验证项 | 结果 |
| :--- | :--- |
| `etc/tinynas/brand` 内容 | `BRAND=tinynas / TIER=pro / DEVICE=n1` |
| `etc/rc.d/S98tinynas-license-check` | ✅ 自动 enable（rc.common shebang） |
| `etc/rc.d/S99tinynas-boot` | ✅ 自动 enable（rc.common shebang） |
| `etc/init.d/tinynas-boot` 头 2 行 | `#!/bin/sh /etc/rc.common` + `START=99` |
| `etc/tinynas/.machine_id` | `40F6598739D3B481` |
| `etc/config/samba4` 含 `TinyNAS` | 2 处 |
| `etc/config/minidlna` 含 `锦盒-媒体库` | 1 处 |
| `www/tinynas-wizard.html` | ✅ 存在 |
| `etc/uci-defaults/50-tinynas` | ✅ 存在 |
| `www/cgi-bin/` | 3 个脚本 |

## 构建日志备注

- `ld.so: libfakeroot.so cannot be preloaded` 警告可忽略（buildroot 内部工具链差异）
- `make image exit=0`，产物完整

## 结论

14 项覆盖层清单的**全部核心链路**（激活劫持→向导→品牌标识→机器码→UCI 默认→Samba/DLNA→CGI→向导页）在真实 IB 环境中通过验证。

## 后续

- [ ] TinyNAS 仓库合并 `feat/overlay-v1` 到 `main`
- [ ] 写入 eMMC 端到端测试（需真机 N1 + U 盘）
- [ ] build-template.sh 适配 `rootfs.tar.gz` 收集逻辑（补充 `openwrt_tinynas-*.rootfs.tar.gz` 命名）
